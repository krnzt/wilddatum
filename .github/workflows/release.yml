name: Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: read

jobs:
  web:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: viewer/web-bootstrap/package-lock.json
      - run: npm ci
        working-directory: viewer/web-bootstrap
      - run: npm run check
        working-directory: viewer/web-bootstrap
      - run: npm run build
        working-directory: viewer/web-bootstrap
      - uses: actions/upload-artifact@v7
        with:
          name: web-explorer
          path: viewer/web-bootstrap/dist
          if-no-files-found: error

  linux-x86_64:
    # Build on the oldest supported hosted image to avoid requiring glibc 2.39.
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v7
      - name: Install Linux build dependencies
        run: sudo apt-get update && sudo apt-get install -y autoconf automake libtool pkg-config
      - uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: "1.95.0"
      - uses: Swatinem/rust-cache@v2
      - run: cargo build --release --locked -p wilddatum-cli
      - run: target/release/wilddatum --version
      - name: Ensure the release does not dynamically depend on libdbus
        run: '! ldd target/release/wilddatum | grep -q libdbus'
      - uses: actions/upload-artifact@v7
        with:
          name: binary-linux-x86_64
          path: target/release/wilddatum
          if-no-files-found: error

  macos-arm64:
    runs-on: macos-15
    env:
      MACOSX_DEPLOYMENT_TARGET: "13.0"
    steps:
      - uses: actions/checkout@v7
      - uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: "1.95.0"
      - uses: Swatinem/rust-cache@v2
      - run: cargo build --release --locked -p wilddatum-cli
      - run: target/release/wilddatum --version
      - uses: actions/upload-artifact@v7
        with:
          name: binary-macos-arm64
          path: target/release/wilddatum
          if-no-files-found: error

  macos-x86_64:
    runs-on: macos-15-intel
    env:
      MACOSX_DEPLOYMENT_TARGET: "13.0"
    steps:
      - uses: actions/checkout@v7
      - uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: "1.95.0"
      - uses: Swatinem/rust-cache@v2
      - run: cargo build --release --locked -p wilddatum-cli
      - run: target/release/wilddatum --version
      - uses: actions/upload-artifact@v7
        with:
          name: binary-macos-x86_64
          path: target/release/wilddatum
          if-no-files-found: error

  package-linux:
    needs: [web, linux-x86_64]
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v7
      - uses: actions/download-artifact@v8
        with:
          name: web-explorer
          path: viewer/web-bootstrap/dist
      - uses: actions/download-artifact@v8
        with:
          name: binary-linux-x86_64
          path: release-input/linux
      - run: chmod +x release-input/linux/wilddatum
      - run: scripts/package-release.sh release-input/linux/wilddatum dist linux-x86_64 linux
      - uses: actions/upload-artifact@v7
        with:
          name: release-linux-x86_64
          path: dist
          if-no-files-found: error

  package-macos:
    needs: [web, macos-arm64, macos-x86_64]
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v7
      - uses: actions/download-artifact@v8
        with:
          name: web-explorer
          path: viewer/web-bootstrap/dist
      - uses: actions/download-artifact@v8
        with:
          name: binary-macos-arm64
          path: release-input/arm64
      - uses: actions/download-artifact@v8
        with:
          name: binary-macos-x86_64
          path: release-input/x86_64
      - name: Create and verify the universal executable
        run: |
          chmod +x release-input/arm64/wilddatum release-input/x86_64/wilddatum
          mkdir -p release-input/universal
          lipo -create release-input/arm64/wilddatum release-input/x86_64/wilddatum -output release-input/universal/wilddatum
          lipo -archs release-input/universal/wilddatum | grep -q arm64
          lipo -archs release-input/universal/wilddatum | grep -q x86_64
          release-input/universal/wilddatum --version
      - run: scripts/package-release.sh release-input/universal/wilddatum dist macos-universal darwin
      - uses: actions/upload-artifact@v7
        with:
          name: release-macos-universal
          path: dist
          if-no-files-found: error

  publish:
    needs: [package-linux, package-macos]
    runs-on: ubuntu-24.04
    permissions:
      contents: write
      id-token: write
    steps:
      - uses: actions/checkout@v7
      - uses: actions/download-artifact@v8
        with:
          name: release-linux-x86_64
          path: dist
      - uses: actions/download-artifact@v8
        with:
          name: release-macos-universal
          path: dist
      - name: Generate the official MCP Registry document
        run: |
          version="${GITHUB_REF_NAME#v}"
          scripts/render-server-json.sh \
            "$version" "$GITHUB_REF_NAME" \
            "dist/wilddatum-v${version}-macos-universal.mcpb" \
            "dist/wilddatum-v${version}-linux-x86_64.mcpb" \
            dist/server.json
      - name: Create prerelease and upload verified artifacts
        uses: softprops/action-gh-release@v3
        with:
          prerelease: true
          generate_release_notes: true
          files: dist/*
      - name: Install MCP Registry publisher
        run: |
          curl -fL --retry 3 \
            https://github.com/modelcontextprotocol/registry/releases/download/v1.8.1/mcp-publisher_linux_amd64.tar.gz \
            | tar xz mcp-publisher
          ./mcp-publisher --version
      - name: Validate and publish to the official MCP Registry
        run: |
          ./mcp-publisher validate dist/server.json
          ./mcp-publisher login github-oidc
          ./mcp-publisher publish dist/server.json
